# 🚀 SQLite - Быстрый старт

## Запуск backend с SQLite

```bash
cd D:\fotobox+react\photobooth-magic-main\backend
python app.py
```

При первом запуске создаётся файл `photobooth.db` (20 KB)

---

## Проверка работы

```bash
curl http://localhost:5000/api/test
```

Ответ:
```json
{
  "database": "SQLite",
  "payments_count": 0,
  "status": "OK"
}
```

---

## Просмотр данных

### Вариант 1: Утилита db_viewer.py

```bash
python db_viewer.py
```

Меню:
```
1. Показать все платежи
2. Показать статистику
3. Детали платежа по ID
4. Экспорт в CSV
5. Очистить базу данных
```

### Вариант 2: SQLite Browser

Скачайте [DB Browser for SQLite](https://sqlitebrowser.org/)

Откройте файл `photobooth.db`

### Вариант 3: Через API

**Все платежи:**
```bash
curl http://localhost:5000/api/payments
```

**Статистика:**
```bash
curl http://localhost:5000/api/stats
```

**Конкретный платёж:**
```bash
curl http://localhost:5000/api/payments/1
```

---

## Тестирование

```bash
python test_api.py
```

Должно пройти 8 тестов:
- ✅ Health check
- ✅ QR генерация (Payme)
- ✅ QR генерация (Click)
- ✅ Проверка статуса
- ✅ Симуляция оплаты
- ✅ Проверка после симуляции
- ✅ Статистика
- ✅ Список платежей

---

## Полезные команды

### Backup базы данных
```powershell
Copy-Item photobooth.db photobooth_backup.db
```

### Экспорт в CSV
```bash
python db_viewer.py
# Выбрать пункт 4
```

### Очистка БД
```bash
python db_viewer.py
# Выбрать пункт 5
```

### Подсчёт платежей
```bash
python -c "import sqlite3; conn = sqlite3.connect('photobooth.db'); print('Total payments:', conn.execute('SELECT COUNT(*) FROM payments').fetchone()[0])"
```

---

## Что дальше?

Читайте полную документацию:
- `SQLITE_MIGRATION.md` - детальное описание миграции
- `README.md` - общая документация API

---

**Готово! База данных работает! 🎉**
